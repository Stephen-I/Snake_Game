import {
  snakeStart,
  canvas,
  directions,
  speed,
  appleStart,
} from "../constants";

const Game = () => {
  const startGame = () => {};
  const endGame = () => {};
  const moveSnake = () => {};

  return;
};

export default Game;
